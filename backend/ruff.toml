# Ruff configuration for DeepProvenance backend
[lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # Line too long

[lint.per-file-ignores]
"alembic/env.py" = ["E402"]  # Module-level imports after config setup (required by Alembic)
